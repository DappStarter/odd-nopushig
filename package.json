{
  "name": "@trycrypto/dappstarter-hypergen",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "lerna run dev --no-sort --stream --parallel"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
